apiVersion: v1
kind: Service
metadata:
  name: {{ include "kibana-deployer.fullname" . }}
  labels:
    {{- include "kibana-deployer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kibana-deployer.selectorLabels" . | nindent 4 }}
